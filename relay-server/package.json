{
  "name": "powerplay-hockey-relay",
  "version": "1.0.0",
  "private": true,
  "description": "WebSocket relay for Power Play Hockey internet multiplayer. Pairs a host and guest by room code and pipes the existing line-delimited JSON match protocol between them verbatim -- it never parses game messages, only two control ones it sends itself (_hello, _bye).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
